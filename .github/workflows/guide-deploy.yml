name: guide-deploy

on:
  push:
    branches: [main]
    paths:
      - 'apps/guide/**'
      - 'tooling/ui/**'
      - 'pnpm-workspace.yaml'
      - '.github/workflows/guide-deploy.yml'
      - '.github/workflows/site-deploy.yml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  deploy:
    uses: ./.github/workflows/site-deploy.yml
    with:
      app: guide
      site_url: https://guide.seedcord.org
    secrets: inherit
