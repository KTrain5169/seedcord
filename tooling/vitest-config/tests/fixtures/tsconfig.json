{
    // the parser must accept jsonc, tsc does
    "compilerOptions": {
        "paths": {
            "#src/*": ["./src/*"],
            "#src/nested/*": ["./nested/*"],
            "#flat": ["./src/flat.ts"],
            "#flat/sub": ["./src/sub.ts"]
        }
    }
}
